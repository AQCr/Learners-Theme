<h1>{{ section.settings.text }}</h1>

{% schema %}
{
    "name": "Heading",
    "tag":"section",
    "settings": [
        {
            "type":"text",
            "label":"Text",
            "id": "text",
            "default": "Heading section"
        }
    ],
    "presets": [
        {
            "name": "Heading"
        }
    ]    
}

{% endschema %}