<h1>{{ section.settings.text }}</h1>

{% schema %}
{
    "name": "Heading",
    "settings": [
        {
            "type":"text",
            "tage":"section",
            "label":"Heading Text",
            "id": "heading",
            "default": "Heading is here"
        }
    ],
    "presets": [
        {
            "name": "Heading"
        }
    ]    
}

{% endschema %}